add to agents:

never run local commands with sudo, if you need any tools installed ask the user to do it

Do both: backfill the conversation's major decisions as numbered ADRs now (they carry real "alternatives considered" value), and from now on every new decision gets an ADR at decision time. Division of labor: behavior.md/architecture.md/web-ui.md = the current truth (what the system is); adr/* = the why (context, options, rejections) — concise, no duplication of details.

After agent has done working on task, ask him to tell/note the decisions he made which he is not confident of.

track "spec/ref/adr.md" with ADRs
